
<!-- === NAMA_FILE: resources/views/checkout/index.blade.php === -->
<x-app-layout>
    <x-slot name="header">
        <h2 class="font-semibold text-xl text-white leading-tight">
            Checkout
        </h2>
    </x-slot>
    
    <form action="{{ route('checkout.process') }}" method="POST">
        @csrf
        <div class="py-6">
            <div class="max-w-sm mx-auto px-4 sm:px-6 lg:px-8 space-y-6">
                
                <!-- Delivery Address -->
                <div class="bg-white dark:bg-gray-800 p-4 rounded-lg shadow-md">
                    <div class="flex justify-between items-center">
                        <h4 class="font-semibold text-gray-900 dark:text-gray-100">Delivery Address</h4>
                        <a href="{{ route('address.index') }}" class="text-sm text-blue-600 dark:text-blue-400">Change</a>
                    </div>
                    @if($defaultAddress)
                    <div class="mt-3 text-sm text-gray-700 dark:text-gray-300">
                        <p class="font-bold">{{ $defaultAddress->nickname }}</p>
                        <p>{{ $defaultAddress->full_address }}</p>
                        <!-- Hidden input untuk kirim address_id -->
                        <input type="hidden" name="address_id" value="{{ $defaultAddress->id }}">
                    </div>
                    @else
                    <div class="mt-3 text-sm text-red-600 dark:text-red-400">
                        <p>Kamu belum punya alamat!</p>
                        <a href="{{ route('address.create') }}" class="font-bold underline">Tambah Alamat Baru</a>
                    </div>
                    @endif
                </div>
                
                <!-- Payment Method -->
                <div class="bg-white dark:bg-gray-800 p-4 rounded-lg shadow-md">
                    <h4 class="font-semibold text-gray-900 dark:text-gray-100">Payment Method</h4>
                    <!-- Sesuai screenshot, ada 3 pilihan -->
                    <div class="mt-3 space-y-3">
                        <label class="flex items-center p-3 border rounded-lg">
                            <input type="radio" name="payment_method" value="card" class="h-4 w-4 text-blue-600 border-gray-300 focus:ring-blue-500" checked>
                            <span class="ml-3 text-sm font-medium text-gray-900 dark:text-gray-100">Card</span>
                            <span class="ml-auto text-sm">**** **** 2912</span>
                        </label>
                        <label class="flex items-center p-3 border rounded-lg">
                            <input type="radio" name="payment_method" value="cash" class="h-4 w-4 text-blue-600 border-gray-300 focus:ring-blue-500">
                            <span class="ml-3 text-sm font-medium text-gray-900 dark:text-gray-100">Cash</span>
                        </label>
                         <label class="flex items-center p-3 border rounded-lg">
                            <input type="radio" name="payment_method" value="apple_pay" class="h-4 w-4 text-blue-600 border-gray-300 focus:ring-blue-500">
                            <span class="ml-3 text-sm font-medium text-gray-900 dark:text-gray-100">Apple Pay</span>
                        </label>
                    </div>
                </div>

                <!-- Order Summary -->
                <div class="bg-white dark:bg-gray-800 p-4 rounded-lg shadow-md space-y-3">
                    <h4 class="text-lg font-semibold text-gray-900 dark:text-gray-100">Order Summary</h4>
                    <div class="flex justify-between text-sm text-gray-700 dark:text-gray-300">
                        <span>Sub-total</span>
                        <span>Rp {{ number_format($subTotal, 0, ',', '.') }}</span>
                    </div>
                    <div class="flex justify-between text-sm text-gray-700 dark:text-gray-300">
                        <span>Biaya Admin</span>
                        <span>Rp {{ number_format($adminFee, 0, ',', '.') }}</span>
                    </div>
                    <div class="flex justify-between text-sm text-gray-700 dark:text-gray-300">
                        <span>Shipping fee</span>
                        <span>Rp {{ number_format($shippingFee, 0, ',', '.') }}</span>
                    </div>
                    <div class="border-t border-gray-200 dark:border-gray-700 my-2"></div>
                    <div class="flex justify-between font-bold text-gray-900 dark:text-gray-100">
                        <span>Total</span>
                        <span>Rp {{ number_format($total, 0, ',', '.') }}</span>
                    </div>
                </div>
                
                <!-- Promo Code -->
                <div class="flex items-center space-x-2">
                    <input type="text" placeholder="Enter promo code" class="flex-1 block w-full border-gray-300 dark:border-gray-700 dark:bg-gray-900 dark:text-gray-300 focus:border-blue-500 dark:focus:border-blue-600 focus:ring-blue-500 dark:focus:ring-blue-600 rounded-md shadow-sm">
                    <button type="button" class="px-6 py-2.5 bg-blue-600 border border-transparent rounded-md font-semibold text-sm text-white hover:bg-blue-500">
                        Add
                    </button>
                </div>

                <!-- Tombol Place Order -->
                <div class="mt-6">
                    <button type="submit" class="w-full flex justify-center items-center px-6 py-3 bg-blue-600 border border-transparent rounded-md font-semibold text-white uppercase tracking-widest hover:bg-blue-500 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition ease-in-out duration-150"
                        @if(!$defaultAddress) disabled @endif>
                        Place Order
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5 ml-2">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3" />
                        </svg>
                    </button>
                </div>

            </div>
        </div>
    </form>
</x-app-layout>

