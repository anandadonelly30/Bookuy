
<!-- === NAMA_FILE: resources/views/notifications/index.blade.php === -->
<x-app-layout>
    <x-slot name="header">
        <h2 class="font-semibold text-xl text-white leading-tight">
            Notifications
        </h2>
    </x-slot>
    
    <div class="py-6">
        <div class="max-w-sm mx-auto px-4 sm:px-6 lg:px-8 space-y-6">
            
            @forelse($notifications as $dateGroup => $items)
                <div class="space-y-4">
                    <h4 class="font-semibold text-gray-700 dark:text-gray-300">{{ $dateGroup }}</h4>
                    
                    @foreach($items as $notification)
                    <div class="flex items-start space-x-4 p-4 rounded-lg {{ $notification->read_at ? 'bg-white dark:bg-gray-800' : 'bg-blue-50 dark:bg-blue-900/30 border border-blue-200' }}">
                        <!-- Icon (Contoh) -->
                        <div class="flex-shrink-0 w-10 h-10 rounded-full bg-blue-100 dark:bg-blue-900 flex items-center justify-center">
                            @if($notification->icon == 'discount')
                            <svg class="w-6 h-6 text-blue-600 dark:text-blue-400" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M9.568 3H5.25A2.25 2.25 0 003 5.25v4.318c0 .597.237 1.17.659 1.591l9.581 9.581c.699.699 1.78.872 2.607.33a18.095 18.095 0 005.223-5.223c.542-.827.369-1.908-.33-2.607L11.16 3.66A2.25 2.25 0 009.568 3z" /><path stroke-linecap="round" stroke-linejoin="round" d="M6 6h.008v.008H6V6z" /></svg>
                            @else
                            <svg class="w-6 h-6 text-blue-600 dark:text-blue-400" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M21 11.25v8.25a2.25 2.25 0 01-2.25 2.25H5.25a2.25 2.25 0 01-2.25-2.25v-8.25M12 4.875A2.625 2.625 0 1012 10.125A2.625 2.625 0 0012 4.875z" /><path stroke-linecap="round" stroke-linejoin="round" d="M18.75 11.25c.621 0 1.125.504 1.125 1.125v2.25c0 .621-.504 1.125-1.125 1.125h-1.5c-.621 0-1.125-.504-1.125-1.125v-2.25c0-.621.504-1.125 1.125-1.125h1.5zM3.75 11.25c.621 0 1.125.504 1.125 1.125v2.25c0 .621-.504 1.125-1.125 1.125h-1.5c-.621 0-1.125-.504-1.125-1.125v-2.25c0-.621.504-1.125 1.125-1.125h1.5z" /></svg>
                            @endif
                        </div>
                        <div class="flex-1 text-sm">
                            <h5 class="font-semibold text-gray-900 dark:text-gray-100">{{ $notification->title }}</h5>
                            <p class="text-gray-600 dark:text-gray-400">{{ $notification->message }}</p>
                        </div>
                        @if(!$notification->read_at)
                        <div class="flex-shrink-0 w-2 h-2 rounded-full bg-blue-500 mt-1"></div>
                        @endif
                    </div>
                    @endforeach
                </div>
            @empty
                <p class="text-gray-500 dark:text-gray-400 text-center py-10">Tidak ada notifikasi.</p>
            @endforelse

        </div>
    </div>
</x-app-layout>
