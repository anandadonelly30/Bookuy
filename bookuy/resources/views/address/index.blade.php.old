
<!-- === NAMA_FILE: resources/views/address/index.blade.php === -->
<x-app-layout>
    <x-slot name="header">
        <h2 class="font-semibold text-xl text-white leading-tight">
            Address
        </h2>
    </x-slot>
    
    <div class="py-6">
        <div class="max-w-sm mx-auto px-4 sm:px-6 lg:px-8 space-y-6">
            
            <h4 class="font-semibold text-gray-900 dark:text-gray-100">Saved Address</h4>
            
            <div class="space-y-4">
                @forelse($addresses as $address)
                <div class="bg-white dark:bg-gray-800 p-4 rounded-lg shadow-md flex items-start space-x-4">
                    <!-- Radio button untuk set default -->
                    <form action="{{ route('address.setDefault', $address) }}" method="POST" class="mt-1">
                        @csrf
                        @method('PATCH')
                        <input type="radio" name="is_default" 
                               class="h-4 w-4 text-blue-600 border-gray-300 focus:ring-blue-500" 
                               {{ $address->is_default ? 'checked' : '' }}
                               onchange="this.form.submit()">
                    </form>
                    
                    <div class="flex-1 text-sm">
                        <p class="font-bold text-gray-900 dark:text-gray-100">{{ $address->nickname }}</p>
                        <p class="text-gray-700 dark:text-gray-300">{{ $address->full_address }}</p>
                    </div>
                    
                    <!-- Tombol Edit/Delete -->
                    <div class="flex space-x-2">
                        <a href="#" class="text-gray-400 hover:text-blue-500">
                            <!-- Edit Icon -->
                            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5"><path stroke-linecap="round" stroke-linejoin="round" d="M16.862 4.487l1.687-1.688a1.875 1.875 0 112.652 2.652L6.832 19.82a4.5 4.5 0 01-1.897 1.13l-2.685.8.8-2.685a4.5 4.5 0 011.13-1.897L16.863 4.487zm0 0L19.5 7.125" /></svg>
                        </a>
                        <!-- Delete Form -->
                        <form action="#" method="POST">
                            @csrf
                            @method('DELETE')
                            <button type="submit" class="text-gray-400 hover:text-red-500">
                                <!-- Delete Icon -->
                                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5"><path stroke-linecap="round" stroke-linejoin="round" d="M14.74 9l-.346 9m-4.788 0L9.26 9m9.968-3.21c.342.052.682.107 1.022.166m-1.022-.165L18.16 19.673a2.25 2.25 0 01-2.244 2.077H8.084a2.25 2.25 0 01-2.244-2.077L4.772 5.79m14.456 0a48.108 48.108 0 00-3.478-.397m-12.54 0c-.27.041-.54.082-.811.124a48.097 48.097 0 00-3.478.397m16.5 0a48.11 48.11 0 00-7.5 0" /></svg>
                            </button>
                        </form>
                    </div>
                </div>
                @empty
                <p class="text-gray-500 dark:text-gray-400 text-center">Belum ada alamat tersimpan.</p>
                @endforelse
            </div>
            
            <!-- Tombol Add New Address -->
            <a href="{{ route('address.create') }}" class="mt-6 w-full flex justify-center items-center px-6 py-3 bg-white dark:bg-gray-800 border border-gray-300 dark:border-gray-700 rounded-md font-semibold text-blue-600 dark:text-blue-400 hover:bg-gray-50 dark:hover:bg-gray-700">
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5 mr-2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M12 4.5v15m7.5-7.5h-15" />
                </svg>
                Add New Address
            </a>
            
            <!-- Tombol Apply -->
             <div class="mt-6">
                <a href="{{ route('checkout.index') }}" class="w-full flex justify-center items-center px-6 py-3 bg-blue-600 border border-transparent rounded-md font-semibold text-white uppercase tracking-widest hover:bg-blue-500 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition ease-in-out duration-150">
                    Apply
                </a>
            </div>

        </div>
    </div>
</x-app-layout>
