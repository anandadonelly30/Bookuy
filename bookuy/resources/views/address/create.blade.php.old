
<!-- === NAMA_FILE: resources/views/address/create.blade.php === -->
<x-app-layout>
    <x-slot name="header">
        <h2 class="font-semibold text-xl text-white leading-tight">
            New Address
        </h2>
    </x-slot>
    
    <div class="py-6">
        <div class="max-w-sm mx-auto px-4 sm:px-6 lg:px-8">
            <form action="{{ route('address.store') }}" method="POST" class="space-y-6">
                @csrf
                
                <!-- Map Placeholder -->
                <div class="h-40 bg-gray-300 dark:bg-gray-700 rounded-lg flex items-center justify-center text-gray-500">
                    [Map Placeholder]
                </div>
                
                <!-- Address Nickname -->
                <div>
                    <x-input-label for="nickname" :value="__('Address Nickname')" />
                    <x-text-input id="nickname" name="nickname" type="text" class="mt-1 block w-full" :value="old('nickname')" required autofocus placeholder="e.g., Home, Office" />
                    <x-input-error class="mt-2" :messages="$errors->get('nickname')" />
                </div>
                
                <!-- Full Address -->
                <div>
                    <x-input-label for="full_address" :value="__('Full Address')" />
                    <textarea id="full_address" name="full_address" rows="3" class="mt-1 block w-full border-gray-300 dark:border-gray-700 dark:bg-gray-900 dark:text-gray-300 focus:border-blue-500 dark:focus:border-blue-600 focus:ring-blue-500 dark:focus:ring-blue-600 rounded-md shadow-sm" required>{{ old('full_address') }}</textarea>
                    <x-input-error class="mt-2" :messages="$errors->get('full_address')" />
                </div>
                
                <!-- Phone Number -->
                <div>
                    <x-input-label for="phone_number" :value="__('Phone Number')" />
                    <x-text-input id="phone_number" name="phone_number" type="tel" class="mt-1 block w-full" :value="old('phone_number')" required />
                    <x-input-error class="mt-2" :messages="$errors->get('phone_number')" />
                </div>
                
                <!-- Set as default -->
                <div class="block">
                    <label for="is_default" class="inline-flex items-center">
                        <input id="is_default" type="checkbox" class="rounded dark:bg-gray-900 border-gray-300 dark:border-gray-700 text-blue-600 shadow-sm focus:ring-blue-500 dark:focus:ring-blue-600 dark:focus:ring-offset-gray-800" name="is_default">
                        <span class="ms-2 text-sm text-gray-600 dark:text-gray-400">{{ __('Set as default address') }}</span>
                    </label>
                </div>
                
                <!-- Tombol Add -->
                 <div class="mt-6">
                    <button type="submit" class="w-full flex justify-center items-center px-6 py-3 bg-blue-600 border border-transparent rounded-md font-semibold text-white uppercase tracking-widest hover:bg-blue-500 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition ease-in-out duration-150">
                        Add
                    </button>
                </div>

            </form>
        </div>
    </div>
</x-app-layout>